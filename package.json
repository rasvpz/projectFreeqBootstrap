{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "backend/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon backend/server.js",
    "frontend": "yarn start --prefix frondend",
    "heroku-postbuild": "yarn install && yarn --cwd ./frondend install && yarn --cwd ./frondend run build"
  },
  "author": "AbduRashid",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "nodemon": "^2.0.18"
  }
}
